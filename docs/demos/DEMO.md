# 🎉 Web UI 功能演示指南

## 快速体验

### 1️⃣ 启动 Web UI

```bash
# 进入项目目录
cd /data/app/claude-history

# 启动守护进程并启用 Web UI
./bin/claude-daemon.sh start --web-ui

# 或使用环境变量自定义端口
WEB_PORT=8080 ./bin/claude-daemon.sh start --web-ui
```

**预期输出**:
```
启动守护进程...
启用 Web UI (端口: 3000)
✓ 守护进程已启动 (PID: 12345)
✓ Web UI: http://127.0.0.1:3000
日志文件: /home/user/.claude/daemon.log
```

### 2️⃣ 访问界面

在浏览器中打开: **http://127.0.0.1:3000**

---

## 🖥️ 界面功能演示

### 顶部导航栏
- **左侧**: Claude Code Dashboard 标题
- **右侧**: 连接状态指示器（绿点 = 已连接）

### 统计卡片（Dashboard）

展示 4 个关键指标：

| 指标 | 图标 | 说明 |
|------|------|------|
| **总会话数** | 💬 | 历史累计所有会话 |
| **活跃项目** | 📁 | 有会话记录的项目数 |
| **平均时长** | ⏱️ | 所有会话平均持续时间 |
| **总时长** | ⚡ | 累计会话总时间 |

### 图表区域

#### 左侧 - 类型分布饼图
显示不同会话类型的占比：
- 🔵 编码 (coding)
- 🔴 调试 (debugging)
- 🟢 研究 (research)
- 🟣 写作 (writing)
- 🟡 Git 操作 (git)
- 🟠 重构 (refactoring)
- ⚫ 混合 (mixed)

**交互**: 点击图例可隐藏/显示特定类型

#### 右侧 - 时间线趋势图
显示最近 30 天的会话活跃度：
- X 轴: 日期（月/日）
- Y 轴: 会话数量
- 悬停查看具体数值

### 过滤器

3 个过滤选项 + 重置按钮：

1. **会话类型** - 下拉选择
   - 全部
   - 编码 / 调试 / 研究 / 写作 / Git / 混合

2. **搜索目录** - 文本输入
   - 输入项目路径快速过滤
   - 示例: `/data/app/claude-history`

3. **显示数量** - 下拉选择
   - 10 / 20 / 50 / 100

4. **重置按钮** - 一键清空所有过滤

### 会话列表

每个会话卡片显示：

```
[类型标签] 2小时前 hostname

/path/to/working/directory

🌿 main                    (Git 分支，如果有)
⏱️ 15分钟  🛠️ 23 工具  ✅ 95% 成功率
```

**交互**: 点击卡片查看详情

**悬停效果**: 卡片向上浮动 2px

### 会话详情模态框

点击会话后弹出，包含：

#### 基本信息（2列布局）
- 会话 ID
- 时间
- 类型
- 时长
- 工作目录

#### 工具使用统计（3列网格）
```
Read        Edit        Bash
  23          12          8

Grep        Write       Glob
   5           3          2
```

#### 修改的文件（滚动列表）
```
src/main.ts
lib/utils.ts
README.md
...
```

#### 会话摘要
自动生成的文字描述

**关闭**: 点击 X 或背景区域

---

## 🔌 API 使用示例

### 查询最近 10 个会话
```bash
curl http://127.0.0.1:3000/api/sessions/recent?limit=10 | jq
```

### 获取编码类型的会话
```bash
curl "http://127.0.0.1:3000/api/sessions/by-type?type=coding" | jq
```

### 查看全局统计
```bash
curl http://127.0.0.1:3000/api/stats/global | jq
```

### 时间线数据
```bash
curl "http://127.0.0.1:3000/api/stats/timeline?days=7" | jq
```

### WebSocket 测试（Node.js）
```javascript
const ws = new WebSocket('ws://127.0.0.1:3000/ws');

ws.onopen = () => console.log('连接成功');

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('收到消息:', data);

  if (data.type === 'session_update') {
    console.log('新会话:', data.data.session_id);
  }
};
```

---

## 🎬 完整演示流程

### 步骤 1: 启动服务
```bash
cd /data/app/claude-history
./bin/claude-daemon.sh start --web-ui
```

### 步骤 2: 打开浏览器
访问 http://127.0.0.1:3000

### 步骤 3: 查看统计
观察仪表板上的统计卡片和图表

### 步骤 4: 测试过滤
- 选择"编码"类型
- 观察列表自动更新

### 步骤 5: 查看详情
- 点击任意会话卡片
- 查看完整的会话信息

### 步骤 6: 测试实时更新
在另一个终端：
```bash
# 创建一个新的 Claude 会话
echo "测试" | claude -p
```

观察 Web UI 是否自动刷新（WebSocket 推送）

### 步骤 7: 停止服务
```bash
./bin/claude-daemon.sh stop
```

---

## 📸 界面截图说明

### 仪表板概览
```
┌────────────────────────────────────────────────┐
│ Claude Code Dashboard          [●已连接]       │
├────────────────────────────────────────────────┤
│  ┌────┐  ┌────┐  ┌────┐  ┌────┐              │
│  │ 50 │  │ 5  │  │ 12 │  │ 8  │              │
│  │会话│  │项目│  │分钟│  │小时│              │
│  └────┘  └────┘  └────┘  └────┘              │
├────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐            │
│  │ [饼图]      │  │ [折线图]    │            │
│  │ 类型分布    │  │ 30天趋势    │            │
│  └─────────────┘  └─────────────┘            │
├────────────────────────────────────────────────┤
│  类型: [▼全部] 目录:[_______] 数量:[▼20] [重置] │
├────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────┐ │
│  │ [编码] 2小时前  myhost                   │ │
│  │ /data/app/project                        │ │
│  │ ⏱️15分 🛠️23工具 ✅95%                    │ │
│  └──────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────┐ │
│  │ [调试] 5小时前  myhost                   │ │
│  │ ...                                       │ │
│  └──────────────────────────────────────────┘ │
└────────────────────────────────────────────────┘
```

### 详情弹窗
```
┌────────────────────────────────────────┐
│ 会话详情                         [×]   │
├────────────────────────────────────────┤
│ 会话ID: abc123        时间: 2小时前    │
│ 类型: 编码            时长: 15分钟     │
│ 目录: /data/app/project               │
├────────────────────────────────────────┤
│ 工具使用统计:                          │
│ [Read: 23] [Edit: 12] [Bash: 8]       │
├────────────────────────────────────────┤
│ 修改的文件:                            │
│ • src/main.ts                          │
│ • lib/utils.ts                         │
│ • README.md                            │
├────────────────────────────────────────┤
│ 会话摘要:                              │
│ 实现了 Web UI 可视化界面...           │
└────────────────────────────────────────┘
```

---

## 🐛 常见问题

### Q: 端口被占用怎么办？
```bash
# 使用其他端口
WEB_PORT=8080 ./bin/claude-daemon.sh start --web-ui
```

### Q: 看不到数据？
```bash
# 检查数据目录
ls ~/.claude/SESSIONS/analysis/summaries/

# 确保有会话记录
claude-sessions recent 5
```

### Q: WebSocket 连接失败？
- 确保守护进程正在运行: `./bin/claude-daemon.sh status`
- 检查防火墙设置
- 查看浏览器控制台错误

### Q: 页面样式错误？
- 检查网络连接（需加载 CDN 资源）
- 清除浏览器缓存
- 使用现代浏览器（Chrome/Firefox/Safari）

---

## 📊 性能监控

### 查看资源占用
```bash
# 查看进程状态
./bin/claude-daemon.sh status

# 实时日志
./bin/claude-daemon.sh logs -f

# 系统资源
ps aux | grep bun
```

### 预期指标
- CPU: < 1% (空闲)
- 内存: ~30MB
- 响应时间: < 10ms

---

## 🎓 学习要点

### 架构亮点
1. **分层清晰** - 展示层、API 层、业务层、数据层
2. **零依赖** - 利用 Bun 内置能力
3. **可选功能** - 不影响 CLI 使用
4. **实时更新** - WebSocket 推送
5. **类型安全** - TypeScript 全栈

### 最佳实践
1. **本地绑定** - 仅监听 127.0.0.1
2. **错误处理** - 完善的异常捕获
3. **优雅关闭** - SIGTERM/SIGINT 处理
4. **日志记录** - 结构化日志输出
5. **响应式设计** - 适配不同屏幕

---

## 🚀 下一步

### 推荐操作
1. ✅ 启动 Web UI 体验所有功能
2. ✅ 测试不同的过滤器组合
3. ✅ 尝试 API 端点调用
4. ✅ 查看 WebSocket 实时更新
5. ✅ 阅读完整文档 (WEB-UI-GUIDE.md)

### 扩展建议
- 自定义端口和主题
- 集成到 CI/CD 流程
- 导出分析报告
- 开发移动端界面

---

**享受全新的可视化体验！** 🎨✨
